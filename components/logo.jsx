import { logo, logo2x, logo3x } from "../public/assets/images";
// TODO: support diferent logo sizes depending on screen size and resolution, in scss file
export const Logo = () => {
    return <img src={logo} alt="Logo" />;
  }
export default Header;